{
  "endpoint": "https://script.google.com/macros/s/PASTE_YOUR_EXEC_URL/exec",
  "anthropicKey": "sk-ant-PASTE_YOUR_KEY",
  "model": "claude-sonnet-5",
  "perSubject": 4,
  "_note": "Copy this file to coach-config.json (which is git-ignored) and fill in the two values. endpoint = your Apps Script /exec URL. anthropicKey = your Claude API key. perSubject = how many new questions to draft per weak subject each week."
}
